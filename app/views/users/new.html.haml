%h1 Create New User

= form_tag users_path do

  = label :user, :user_id, 'User-up'
  = text_field :user, 'user_id'

  = label :user, :email, 'E-Mail'
  = text_field :user, 'email'

  = submit_tag 'Create my account'
